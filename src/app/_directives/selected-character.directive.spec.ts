import { SelectedCharacterDirective } from './selected-character.directive';

describe('SelectedCharacterDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedCharacterDirective();
    expect(directive).toBeTruthy();
  });
});
